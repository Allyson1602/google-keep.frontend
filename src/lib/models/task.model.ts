export interface ITaskView {
    title: string;
    description: string;
    done: boolean;
}

export interface ITaskModel {
    id: number;
    title: string;
    description: string;
    done: boolean;
}